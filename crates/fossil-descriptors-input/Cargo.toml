[package]
name = "fossil-descriptors-input"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Input descriptors (CSVW, JSON Schema, Parquet, XSD) for forward type propagation"

[lints]
workspace = true

[dependencies]
# Phase 1: trait + minimal CSV stub. csv / serde_json / roxmltree / jsonschema
# stay deferred to Phase 3 CORE-05 + Phase 5 STDL-06 (already declared at
# workspace level for inheritance).
indexmap = { workspace = true }
smol_str = { workspace = true }
thiserror = { workspace = true }
