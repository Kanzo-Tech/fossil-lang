[package]
name = "fossil-registry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Stdlib function registry (pure-SQL vs native-UDF classification)"

[lints]
workspace = true

[dependencies]
# Phase 1: standalone data — no fossil-base dep yet.
# Phase 5 STDL-01..07 may add fossil-mir/fossil-codegen when entries carry codegen hints.
smol_str = { workspace = true }
