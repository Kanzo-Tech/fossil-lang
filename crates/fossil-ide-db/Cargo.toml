[package]
name = "fossil-ide-db"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Symbol indexes and analysis primitives shared between IDE and LSP"

[lints]
workspace = true

[dependencies]
# Phase 6 LSP-01 (plan 06-03): cross-file symbol/prefix/workspace indexes
# (rust-analyzer `ide-db` pattern). All three deps below are WASM-clean —
# fossil-ide-db is in the 9-crate WASM gate (06-01). No native-only deps
# (no lsp-server, no tokio) may enter this crate.
fossil-base = { path = "../fossil-base" }
fossil-syntax = { path = "../fossil-syntax" }
smol_str = { workspace = true }
