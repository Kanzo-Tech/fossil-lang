[package]
name = "fossil-ide-db"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Symbol indexes and analysis primitives shared between IDE and LSP"

[lints]
workspace = true

[dependencies]
# Phase 1: empty placeholder. Phase 6 LSP-01 wires symbol indexes
# (rust-analyzer ide-db pattern) and adds salsa + fossil-base + fossil-syntax + fossil-hir.
