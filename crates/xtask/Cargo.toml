[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false
description = "Repository automation — run via `cargo xtask <command>`."

[dependencies]
serde_json = { workspace = true }
