[package]
name = "fossil-wasm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "WASM shim for the Fossil compiler — exposes FossilPlayground to JS"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false
test = false

[lints]
workspace = true

[dependencies]
# Compiler-core crates wired into FossilPlayground::compile per RESEARCH.md
# Pattern 5 (lines 466-484): the Phase 1 wasm dep tree is exactly the 5
# compiler crates plus WASM glue. NO fossil-runtime (native DuckDB), NO
# fossil-cli/fossil-lsp (binaries), NO fossil-sinks (heavy arrow/parquet —
# Phase 1 manifest is hand-templated in fossil-codegen).
fossil-base    = { path = "../fossil-base" }
fossil-syntax  = { path = "../fossil-syntax" }
fossil-hir     = { path = "../fossil-hir" }
fossil-mir     = { path = "../fossil-mir" }
fossil-codegen = { path = "../fossil-codegen" }
# Plan 03-03 Task 2 step 6: WASM host wires `SystemWithDescriptors` on its
# `WasmSystem` struct. Default `output_descriptor_kind()` returns AcceptAll
# for Phase 3 v0.1 — Phase 7+ playground wires user-supplied ShEx text.
fossil-descriptors-output = { path = "../fossil-descriptors-output" }

# WASM glue — wasm-bindgen exact-pinned at workspace level (=0.2.120) per
# RESEARCH.md Pitfall 5: the lib version MUST match the wasm-bindgen-cli
# version installed locally / in CI.
wasm-bindgen             = { workspace = true }
serde-wasm-bindgen       = { workspace = true }
serde                    = { workspace = true }
serde_json               = { workspace = true }
js-sys                   = { workspace = true }
console_error_panic_hook = { workspace = true }

# Transitive WASM enablers — keep as direct deps so feature resolution sticks.
# The `wasm_js` feature on getrandom is required by salsa→hashbrown→ahash on
# wasm32; the `.cargo/config.toml` rustflag is the belt-and-suspenders fallback
# for unconditioned getrandom call sites.
getrandom = { workspace = true }
uuid      = { workspace = true }

[dev-dependencies]
wasm-bindgen-test.workspace = true
