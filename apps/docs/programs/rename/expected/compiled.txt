── type bindings ──
Person ← mixed.shex = https://shop.example/voc#Person

── source bindings ──
User := io.csv(data/users.csv)

── mappings ──
People: 3 written, 3 lowered
    @subject
    name
    foaf_name

── run ──
vertex Person (https://shop.example/voc#Person) × 3 — dense_id, subject, name, foaf_name, x, y, cluster_id
NO EDGE TYPE was written
