── type bindings ──
Person ← two-mappings.shex = https://shop.example/voc#Person

── source bindings ──
User := io.csv(data/users.csv)
Adults from User.where(User.age >= 18)
Minors from User.where(User.age < 18)

── mappings ──
Adults: 3 written, 3 lowered
    @subject
    email
    name
Minors: 3 written, 3 lowered
    @subject
    email
    name

── run ──
vertex Person (https://shop.example/voc#Person) × 3 — dense_id, subject, email, name, x, y, cluster_id
NO EDGE TYPE was written
