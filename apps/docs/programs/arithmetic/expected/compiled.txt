── type bindings ──
Line ← arithmetic.shex = https://shop.example/voc#Line

── source bindings ──
Row := io.csv(data/lines.csv)

── mappings ──
Lines: 3 written, 3 lowered
    @subject
    net
    payable

── run ──
vertex Line (https://shop.example/voc#Line) × 3 — dense_id, subject, net, payable, x, y, cluster_id
NO EDGE TYPE was written
