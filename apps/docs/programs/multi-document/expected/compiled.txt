── type bindings ──
Persona ← es.shex = https://ayto.example/voc#Persona
Human ← en.shex = https://council.example/voc#Person

── source bindings ──
Padron := io.csv(data/padron.csv)
Roll := io.csv(data/roll.csv)

── mappings ──
Personas: 2 written, 2 lowered
    @subject
    nombre
Humans: 2 written, 2 lowered
    @subject
    name

── run ──
vertex Persona (https://ayto.example/voc#Persona) × 2 — dense_id, subject, nombre, x, y, cluster_id
vertex Person (https://council.example/voc#Person) × 2 — dense_id, subject, name, x, y, cluster_id
NO EDGE TYPE was written
