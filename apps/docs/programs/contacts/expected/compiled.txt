── type bindings ──
Person ← contacts.shex = https://shop.example/voc#Person

── source bindings ──
Contact := io.csv(data/contacts.csv)
Reachable := Contact.where(Contact.email != null)

── mappings ──
Contacts: 3 written, 3 lowered
    @subject
    email
    name

── run ──
vertex Person (https://shop.example/voc#Person) × 2 — dense_id, subject, email, name, x, y, cluster_id
NO EDGE TYPE was written
