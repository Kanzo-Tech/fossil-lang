── type bindings ──
Batch ← scaling.shex = https://lab.example/voc#Batch

── source bindings ──
Row := io.csv(data/batches.csv)

── mappings ──
Batches: 5 written, 5 lowered
    @subject
    subtotal
    unit_cost
    leftover
    charged

── run ──
vertex Batch (https://lab.example/voc#Batch) × 3 — dense_id, subject, subtotal, unit_cost, leftover, charged, x, y, cluster_id
NO EDGE TYPE was written
