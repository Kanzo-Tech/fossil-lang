  × `Users` and `Imported` mint two identities for Person
    ╭─[program.fossil:7:16]
  6 │ Users : Person from User
  7 │     @subject = "https://shop.example/user/{User.email}"
    ·                ────────────────────┬───────────────────
    ·                                    ╰── `Users` mints this one
  8 │     name     = User.name
  9 │ 
 10 │ Imported : Person from Legacy
 11 │     @subject = "https://shop.example/user/{Legacy.account_id}"
    ·                ───────────────────────┬───────────────────────
    ·                                       ╰── `Imported` mints this one
 12 │     name     = Legacy.full_name
    ╰────
  help: a type has one identity. Every mapping that produces Person writes the same @subject, so
        that an edge naming Person reaches the same node whichever mapping emitted it.

