── type bindings ──
Contact ← disjunction.shex = https://shop.example/voc#Contact

── source bindings ──
Row := io.csv(data/contacts.csv)
Reachable := Row.where((Row.email != null) or (Row.phone != null))

── mappings ──
Contacts: 3 written, 3 lowered
    @subject
    name
    priority

── run ──
vertex Contact (https://shop.example/voc#Contact) × 3 — dense_id, subject, name, priority, x, y, cluster_id
NO EDGE TYPE was written
