# Keep the build context small: the Dockerfile only needs Cargo sources to build
# `fossil` + `fossil-mcp`. Everything below is rebuilt inside the image or irrelevant.
target/
**/node_modules/
**/pkg/
**/dist/
.git/
.github/
apps/
packages/
.planning/
decisions/
spikes/
**/*.tgz
