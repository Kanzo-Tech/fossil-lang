// @kanzo/theme test setup — happy-dom is configured in
// ../../vitest.config.base. The Provider's React render path needs no
// extra setup (no Radix/ResizeObserver dependencies — just a div + inline
// style). Intentionally empty.
export {};
