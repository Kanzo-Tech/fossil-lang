/**
 * @fossil-lang/ui — public entry.
 *
 * Primitives + tokens land in Phase 10 plans 10-02 (tokens) and 10-03 /
 * 10-04 / 10-05 (primitives). See ADR-0033 (package decision) +
 * ADR-0034 (CSS variable naming convention).
 *
 * Intentionally empty in 10-01 scaffold — downstream Wave-2 plans append
 * to this barrel as primitives land.
 */
export {};
