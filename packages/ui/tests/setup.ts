// @fossil-lang/ui test setup — happy-dom is configured in
// ../../vitest.config.base. No additional setup needed until primitives
// land in 10-03/04/05 (Radix primitives need ResizeObserver / matchMedia
// stubs in happy-dom — added then, not now).
//
// Intentionally empty: the smoke test in tests/smoke.test.ts only exercises
// the cx() utility + the empty barrel.
export {};
