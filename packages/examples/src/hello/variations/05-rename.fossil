// PLAY-05 variation: 05-rename — same mapping, source binding renamed
// from `users` to `people`. MUST succeed (semantics unchanged).

prefix ex: <https://example.org/>

people := io.csv("@examples/hello.csv")

User : ex:Person from people
    iri = `${ex:}user/${.id}`
    ex:name = .name
